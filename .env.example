# Local development uses SQLite by default automatically.
# In production on Vercel, connect a Neon Postgres database or set DATABASE_URL / POSTGRES_URL:
# DATABASE_URL=postgresql://user:password@ep-xyz.us-east-2.aws.neon.tech/neondb?sslmode=require
